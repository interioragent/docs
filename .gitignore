# Not sure what a .gitignore is?
# See: https://git-scm.com/docs/gitignore

# These are directly copied from Jekyll's first-party docs on `.gitignore` files:
# https://jekyllrb.com/tutorials/using-jekyll-with-bundler/#commit-to-source-control

# Ignore the default location of the built site, and caches and metadata generated by Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ignore folders generated by Bundler
.bundle/
vendor/
tmp/

# macOS junk
.DS_Store

# Source .blend files used to generate the figure renders (large; not needed for the site).
# Only the rendered PNGs under assets/scenes/ are committed.
assets/scenes/blend/

# Locally generated standalone HTML preview (regenerate with build_preview.py)
preview.html